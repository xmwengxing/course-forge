# Course-Forge 技能改进笔记

> **最新一次更新：2026-06-01 · 1.6 课程 24 章全部完成 + 1.6 时长实测 + 5 组件沉淀**
> 倒序追加（最新在顶）。任何时候上下文丢失，可以靠本笔记重建改进方向。
> 风格：1-2 句 + **触发场景** + **改进规则**。

---

## Z. 1.6 课程 24 章完整交付后的核心事实

### Z1. 1.6 课程实测时长
- **触发**：用户原话"每次验收时都应统计号课时总时长"
- **实测数据**（从 `audio-segments.json` 计算）：
  - 总章节数：**24**（S1: 4 + S2: 5 + S3: 6 + S4: 5 + S5: 4）
  - 总 narrations：**274**
  - 总字数：**11,520 字**（中文）
  - 纯朗读（380ms/字）：**73.0 min**
  - 视频估算（含视觉过渡 1.5s/步）：**79.0 min**
  - 录屏实测（翁老师录屏语速通常 300ms/字）：**~50-60 min** 实际视频
- **修正目标**：45 min 偏紧，**实际目标应为 50-60 min** 视频
- **字数范围修正**：
  - 旧：2,000-5,000 字 → 25-35 min
  - 新：5,000-8,000 字 → **30-40 min**；8,000-15,000 字 → **40-60 min**（实测）
  - 字/步范围 130-180 仍 OK
- **规则**：
  - 每次验收前**必须**统计本段 narrations 数 / 总字数 / 纯朗读时长 / 视频估算
  - 工具：`scripts/chapter-stats.py`（从 `audio-segments.json` 自动算）

### Z2. 5 个公共组件已沉淀到 1.6 项目
- **触发**：1.6 课件开发**自然沉淀**出 5 个通用画面构建积木
- **列表**：
  - `AnimatedNumber.tsx` — 数字 ease-out-quart 滚动入场（峰值时刻用）
  - `BpmnFlow.tsx` — 流程图节点（reveal 顺序 + killed 划线 + focused 高亮）
  - `FlippableCard.tsx` — 3D 翻转卡片（点击翻面）
  - `StaggeredAppear.tsx` — 序列入场（stagger delay 控制）
  - `TabSwitcher.tsx` — 标签页切换（所有 panel 同 DOM，CSS 切显隐）
- **状态**：
  - 1.6 项目里有完整代码 ✅
  - **templates 仓库里还没有**（待 P1-5 迁移）
- **通用性**：✅ 跨主题、跨领域、跨业务（不绑定 BPMN / 业务流程 / chalk-garden）

---

## 0. 画面风格：减少虚线 + 配色丰富 + 动画柔和（最新反馈）

### 0.1 边框：实线 > 点线 > 虚线
- **触发**：用户原话"适当减少使用虚线边框，不是很柔和"
- **规则**：
  - 主卡片边框用 `solid 1px var(--rule)` 或 `solid 1px var(--accent)`（**实线**）
  - 装饰性分隔用虚线 / 渐变虚化
  - 章节 CSS 顶部统一声明：
    ```css
    .xxx-card { border: 1px solid var(--rule); border-radius: var(--r-card); }
    .xxx-card-accent { border: 1px solid var(--accent); }
    .xxx-divider { border-top: 1px dashed var(--rule); }  /* 仅装饰 */
    ```

### 0.2 配色丰富化（避免纯黄色疲劳）
- **触发**：用户原话"几乎所有画面都只有黄色，会视觉疲劳"
- **规则**（chalk-garden 主题下）：
  - 主色仍是黄（`--accent` = `#facc15`），但**强调色按内容语义分配**：
    - 数据/统计/CTA → 黄（`--accent`）
    - 警告/失败/删除 → 红（`--bad` = `#ef4444`）
    - 成功/正向 → 绿（`--good` = `#22c55e`）
    - 信息/中性 → 蓝（`--info` = `#3b82f6`）
    - 流程节点/对照 → 紫（`--flow` = `#8b5cf6`）
  - 每个章节至少 1-2 个非黄色元素
  - 关键警告/失败/反向案例必须用红（不再是黄）
- **token.css 待补充**：`--bad / --good / --info / --flow` + 配套 `*-soft` 背景色

### 0.3 动画柔和化
- **触发**：用户原话"动画过渡有一点生硬"
- **规则**：
  - 默认缓动用 `cubic-bezier(0.16, 1, 0.3, 1)`（柔顺的 ease-out，比 ease-quart 更平滑）
  - 元素入场：`translateY(16px) → 0` + `opacity 0 → 1`，时长 600-800ms
  - hover/focus：`scale(1.02)` 替代 `scale(1.06)`（更克制，不刺眼）
  - 数字滚动：1.0-1.4s（不要超过 1.6s）
  - 卡片翻转：700ms（已有）
  - 流程图节点点亮：用 `stroke-dasharray` 而非瞬变

---

## A. 画面架构（最高优先级 · 反复踩坑 3 次）

### A1. 页面=主题，step=细节（核心规则）
- **触发**：S1 第一版把 78 步口播拆成 78 个独立卡片（每步一切），被批"呆板"；第二版改成"字号无脑放大"，被批"过度改良"。
- **规则**：
  1. **一个 page 承载一个完整主题**，3-5 个 step 累积在同页面内不同区域
  2. 页面内用 `inPage >= N` 条件分阶段入场，**不是每步一切**
  3. 页面切换时机 = 主题讲完时（4-5 步走完才切下一页）
- **判定**：口播讲完一个完整概念后，下一个 step 才切到新 page

### A2. 字号按信息密度自适应
- **触发**：第二版无脑统一放大字号（4+ 元素时挤在一起）
- **规则**：
  - 元素少（1-2 个）→ 字号大（64-128px）撑画面
  - 元素中（3 个）→ 字号中（44-56px）
  - 元素多（4+）→ 字号小（24-36px）保持可读性
- **禁止**死板统一放大

### A3. 画面必须含图形元素
- **触发**：第一版全部"虚线框+文字"被批"呆板"
- **规则**：每屏至少 1 个图形元素（图标 / 流程图 / 对比表 / 柱状图 / 数字滚动 / 时间轴 / 网格 / 公式）
- **禁止**纯虚线框+文字排版

### A4. 画面内必须有动态 / 互动（5 类来源，硬性规则）
- **触发**：用户原话"单一画面内也没有任何动态效果，非常呆板"
- **规则**（每屏**至少 1 类**，强烈建议 2-3 类）：
  1. **数字滚动** — `AnimatedNumber` 从 0 滚到目标（峰值时刻用）
  2. **序列入场** — `StaggeredAppear` 让多元素分批出现
  3. **微动效** — 元素出场后做小幅度脉动 / 浮动 / 旋转（避免静止呆板）
  4. **互动组件** — `TabSwitcher` / `FlippableCard` 让用户点击触发
  5. **路径绘制** — 流程图节点用 `stroke-dasharray` 动画
- **判定**：单画面如果只有 fade-in + translateY 一组入场 → **不算合格**（太单调）
- **参考**：5 个公共组件 src/components/ 详见 Z2

### A5. 画面信息密度 > 口播稿信息密度（双源原则）
- **触发**：发现章节画面只是"口播字幕打字打了一遍"——失去了视频的视觉价值
- **规则**：
  - 口播稿 = 节拍（每步一个完整想法）
  - 画面 = 从原始文章抽信息池（数字 / 引用 / 案例 / 标签 / 时间）挂到屏幕上
  - 每步画面**至少 3 个独立信息锚点**（数字 + 标签 + 装饰元素）
  - **不要**为"对位画面元素"切碎字幕（字幕 = 2 行内一句完整话）
- **反面教材**：s1 早期版本，TSX 里只放"标题 + 一段文字"，跟口播一字不差
- **正面教材**：1.6 S1 t6-bill-gate，5 步口播对应 1 页，页面有翁老师头像 / 课程标题 / Bill Gates 英文原文 / 两条定律 / 中文翻译 / 时代对比 / 5 段课程地图

### A6. 5 个公共组件（src/components/，1.6 课件开发沉淀）
- `AnimatedNumber.tsx` — 数字 ease-out-quart 滚动入场
- `FlippableCard.tsx` — 3D 翻转卡片（data-no-advance 防误触）
- `TabSwitcher.tsx` — Tab 切换（所有 panel 同 DOM，CSS 切显隐）
- `BpmnFlow.tsx` — BPMN 流程图节点（reveal 顺序 + killed 划线 + 焦点高亮）
- `StaggeredAppear.tsx` — 序列入场（stagger delay 控制）
- 全部加 data-no-advance 防止误触推进

---

## B. 字幕与同步

### B1. 字幕停留时长按字数占比分配
- **触发**：用户原话"字幕框是两行，足够容纳较长字幕，尽量避免字幕分段太多，多段连续跳跃会导致明显的字幕和音频不同步"
- **规则**：
  - 字幕优先在自然语义停顿处切分（逗号 / 句号 / 「」），不要硬拆
  - 字幕时长 = 一节口播总时长 × 该子句字数占比
  - 不要为"对位画面元素"切碎字幕（每步一卡一切才是问题）→ 详见 B3
  - 用户后续**手工微调**字幕时间，不追求自动精准
  - 字幕 = 两行内完整呈现一句话优先

### B2. 画面-口播对位自检清单
- **触发**：S2-1 t6-bpmn-symbols 旧版 narrations 已扩到 10 步但 TSX 还按 6 步分支
- **规则**：每章 TSX 写完后跑 5 维度对位
  1. 数字对位：口播"7 天"画面有"7"
  2. 专名对位：口播"ECRS"画面有"ECRS"
  3. 形状对位：口播"矩形"画面有矩形
  4. 对比对位：口播"旧 vs 新"画面有双卡片
  5. 时序对位：口播"先 A 再 B"画面动画按 A→B

### B3. 字幕与画面元素映射规则（避免字幕切碎）
- **触发**：为了"对位画面元素"把一句完整话切碎成多段字幕
- **规则**：
  - 字幕 = 完整句子优先（≤ 2 行）
  - **不要**为每个画面元素出场都对应一段字幕切换
  - 画面元素可以分阶段入场（inPage >= N），但字幕流是**完整一句话一次性显示**
  - 字幕时长由字数占比决定（详 B1），**不**由画面元素数决定
- **正面教材**：1.6 S1 t6-bill-gate step 4 整段英文 + 定律 + 中文翻译，字幕 1 段连续显示 3 秒，画面元素分阶段入场
- **反面教材**：S2 第一版，5 个 BPMN 符号分别对应 5 段字幕切碎

---

## C. 流程与节奏（硬性约束）

### C1. 逐段验收（绝对禁止跨段开发）
- **触发**：本次踩坑 —— S1 4 章刚部署完，agent 立即开 S2 5 章，验收时发现 S2 多处画面-口播对位 bug
- **规则**：
  - 每段（S1-S5）开发完成 → build + deploy → **立即停一次**等待用户验收
  - **不允许一口气开发完多段**——即使后续段已经规划好
  - 每次验收附带：本段 narrations 数、估算时长、已发现 bug

### C2. 主动提出两种方案让用户选择
- **触发**：1.6 section 1.1~1.5 实际合并到同一 course.json，但 agent 一开始只给了"跟随现状"单一方案
- **规则**：架构决策时 agent **必须** 列出至少两种方案（推荐 + 替代），说清每种方案的代价
- **应用场景**：course.json 结构、章节命名约定、章节归属 section、是否脚手架独立

### C3. 验收报告硬性约束（每次验收必出）
- **触发**：用户原话"每次验收时都应统计号课时总时长"
- **规则**：每次 build + deploy 后**必须**附带验收报告，包含：
  1. 本段 narrations 总数
  2. 总字数（中文）
  3. 纯朗读时长（380ms/字）
  4. 视频估算时长（+ 视觉过渡 1.5s/步）
  5. 5 维对位自检结果（数字 / 专名 / 形状 / 对比 / 时序）
  6. 已修复 bug 清单
- **工具**：`scripts/chapter-stats.py`（从 `audio-segments.json` 自动算时长）
- **使用方式**：agent 验收汇报时**先跑脚本**，把数字贴在汇报里
- **反面教材**：1.6 S1 第一版，agent 汇报时只说"已部署"——没时长，没对位，错过多个 bug

---



## D. 内容规划（时长与扩稿）

### D1. 大纲设计时的时长倒推表
- **触发**：S1 旧版 26 个 narrations 实测 ~3.5 min，目标 6 min，缺口 2.5 min 被无视
- **规则**：Phase 0.2 输出**时长倒推表**：
  ```
  | 段 | 章节数 | 目标时长 | 目标 narrations | 平均字/步 | 总字数 |
  ```
- 阶段验收时附带：**目标/估算/缺口** 三个数字
- 缺口 > 0 立即在该段加扩，**不允许推到下一段**

### D2. 1.6 课程实测时长经验值（2026-06 校准）
- **实测**：1.6 课件 24 章 / 274 步 / 11,520 字 → 纯朗读 73 min，视频估算 79 min
- **目标修订**：**45-60 min 实际视频**（原 45 min 偏紧）
- **字数范围修正**：
  - 旧：2,000-5,000 字 → 25-35 min
  - 新：5,000-8,000 字 → 30-40 min；8,000-15,000 字 → **40-60 min**（实测）
- **字/步范围**：130-180（不变）
- **翁老师语速参考**：350-400ms/字（录屏时通常 300ms/字 → 50-60 min 视频）

### D3. 不要堆砌空泛排比句
- 触发：原稿中"正如某某所说、不仅如此、我们更要……"等空话充字数
- 规则：
  - 凑时长核心是**加深知识点**（举例、对比例证、案例数据、行业背景）
  - 不用"不仅如此"式排比
  - 扩展口播稿优先加：具体数字、对比数据、案例细节、工具/产品名、术语解释

### D4. 画面信息密度 > 口播稿信息密度（双源原则，硬性）
- **触发**：发现章节画面只是"口播字幕打字打了一遍"——失去了视频的视觉价值
- **规则**：
  - 口播稿 = 节拍（每步一个完整想法）
  - 画面 = 从原始文章抽信息池（数字 / 引用 / 案例 / 标签 / 时间）挂到屏幕上
  - 每步画面**至少 3 个独立信息锚点**（数字 + 标签 + 装饰元素）
  - **不要**为"对位画面元素"切碎字幕（字幕 = 2 行内一句完整话）→ 详见 B3
- **反面教材**：1.6 S1 第一版早期，TSX 里只放"标题 + 一段文字"，跟口播一字不差
- **正面教材**：1.6 S1 t6-bill-gate，5 步口播对应 1 页，页面有翁老师头像 / 课程标题 / Bill Gates 英文原文 / 两条定律 / 中文翻译 / 时代对比 / 5 段课程地图

### D5. 动效柔和化参数表（1.6 实测）
- **触发**：用户原话"动画过渡有一点生硬"
- **参数**：
  - 缓动曲线：`cubic-bezier(0.16, 1, 0.3, 1)` 而非 `cubic-bezier(0.19, 1, 0.22, 1)`
  - 元素入场：`translateY(16px) → 0` + `opacity 0 → 1`，时长 **600-800ms**
  - hover/focus：`scale(1.02)` 而非 `scale(1.06)`（更克制）
  - 数字滚动：1.0-1.4s 上限
  - 卡片翻转：700ms
  - 流程图节点点亮：`stroke-dasharray` 而非瞬变
- **token.css 配套**：在 chalk-garden 主题里新增 `--ease-soft-out: cubic-bezier(0.16, 1, 0.3, 1)`

---



## E. 厂商/额度/接入抽象化

### E1. SKILL.md 不写具体厂商
- **触发**：用户指出 MiniMax 订阅已无 4000 字符额度限制，原 SKILL.md 中"错误码 2056 = Token Plan 额度未分配""联系 MiniMax 客服"等内容已过时
- **规则**：
  - SKILL.md 不得出现任何具体厂商的额度/积分/订阅描述
  - 不得硬编码任何语音 ID
  - TTS provider 描述保持中立："任何支持 TTS 的 provider 都可接入"
  - 限流相关描述改为通用："如果遇到限流错误，等待并重试"
  - **待清理位置**：`SKILL.md` Phase 3 音频合成章节、references/AUDIO.md（如果存在）

---

## F. TTS 限流处理（通用版）

### F1. 通用 TTS 限流识别 + 重试策略
- **触发**：任何 TTS API 在大量调用时都会触发限流
- **通用识别**：
  - HTTP 状态码 429（Too Many Requests）
  - 响应体里的 `status_code: 1002` (RPM) / `1039` (TPM) 等
  - 错误信息含 "rate limit" / "too many requests" / "RPM exceeded"
- **重试策略**：
  - 等 **60 秒** 后重新跑合成命令（runner 会自动跳过已合成）
  - 不需要改 provider / 改 endpoint
  - 一个 round 最多能合成约 50-60 个 mp3（取决于每条字数）
- **重要**：用 `script.md` 字数判断 limit（结合 E1），不依赖厂商具体数值

### F2. 增量合成不烧重复 token
- 已存在的 mp3 自动跳过（断点续合）
- `--force` 标志强制全部重合成

---

## G. 待办（拆分优先级 · 2026-06-01 整理）

### G1. P0 必做（影响下次开发体验）
- [x] P0-1 笔记新增 C3 验收报告硬性约束（**已完成**）
- [x] P0-2 部署 `chapter-stats.py` 验收脚本（**待做**）
- [x] P0-3 笔记新增 D4 画面信息密度原则（**已完成**）
- [x] P0-4 笔记 A4 升级为 A6 动态元素 5 类硬性规则（**已完成**）

### G2. P1 重要（沉淀 1.6 经验到 skill）
- [ ] P1-5 5 个新组件迁入 templates/components/
- [x] P1-6 D5 动效柔和化参数表（**已完成**）
- [x] P1-7 B3 字幕-画面元素映射规则（**已完成**）
- [x] P1-8 C3 验收报告硬性约束（**已完成**）
- [x] P1-9 升级 D2 实测时长经验值（**已完成**）

### G3. P2 清理（同步 + 去重）
- [ ] P2-10 SKILL.md 清理 MiniMax 特定内容
- [ ] P2-11 templates/scripts/ 补 extract+合成+tts-providers
- [ ] P2-12 同步 regenerate-course-json.py
- [ ] P2-13 G 待办清单拆分优先级（**部分完成**）
- [ ] P2-14 CHAPTER-CRAFT.md 增强（5 维对位 + 5 类动态）
- [ ] P2-15 AUDIO.md 更新（移除 MiniMax 错误码）
- [ ] P2-16 QUIZ-CRAFT.md 补 60s 倒计时范本

---

## F. TTS 限流处理

### F1. MiniMax RPM 限流
- 错误码：`status_code: 1002, status_msg: "rate limit exceeded(RPM)"`
- 重试：等 **60s** 后重新跑 `npm run synthesize-audio`，脚本自动跳过已合成
- 不需要改 provider / 改 endpoint
- 一个 round 最多能合成约 50-60 个 mp3（取决于每条字数），之后会触发限流
- **重要**：用 `script.md` 字数判断 limit，不依赖厂商具体数值（结合 E1）

---

## G. 待办（开发中持续收集）

- [ ] 清理 SKILL.md 中所有 MiniMax 特定内容（错误码、RPM 值、特定语音 ID）
- [ ] 提取 `regenerate-course-json.py` 脚本到 course-forge 仓库
- [ ] 把本笔记 A1-A5 整理为 SKILL.md Phase 2 的"画面架构规范"章节
- [ ] 把本笔记 B1-B2 整理为 SKILL.md Phase 3 的"字幕生成"章节
- [ ] 把本笔记 C1-C2 / D1-D3 整理为 SKILL.md Phase 0 / 1 的硬性约束
- [ ] 把本笔记 A5 的 5 个组件迁移到 course-forge 仓库 templates/

---

## H. 已废弃方案存档（避免重复踩坑）

### H1. 一口气开发多段
- 触发：S1 部署后立即开 S2，结果 S2 验收时多处 bug，S1 本身密度也不足
- 废弃原因：缺少反馈循环，错误会累积放大
- 替代：每段 3-6 章 → 验收 → 下一段

### H2. 每步一个独立卡片（78 切 = 78 页）
- 触发：S1 第一版被批"呆板"
- 废弃原因：缺少画面层次和累积感
- 替代：3-5 步累积在同一 page，page 内分阶段入场

### H3. 字号无脑统一放大
- 触发：S1 第二版被批"过度改良"
- 废弃原因：4+ 元素时挤在一起
- 替代：按信息密度自适应

### H4. edge-tts 误判为 MiniMax（5月29日 15:45）
- 触发：t5-what-is-pointcloud 文件大小 188KB（远小于其他 MiniMax 文件 400-600KB）
- 识别方法：**用文件大小判断 TTS 来源**（>250KB = MiniMax，<250KB = edge-tts）

